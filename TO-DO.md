1. uv init project ☑️
2. activate venv ☑️
3. install `RL Gymanium` Library ☑️
4. install `notebook` Library ☑️
5. install `matplotlib` Library ☑️
6. install `RWARE` environment ☑️
7. set up environment ☑️
8. display image of environment [bug]
    - write custom wrapper render function that returns only array that can be displayed ☑️
    # Note partially fixed we can retrieve actual images only but it keeps poping up, is there a way to make it? not pop up


9. display action space ☑️
10. display observation space ☑️
11. move a single agent ☑️
12. make small record of random agent script playing, save it as a video ☑️
13. see how would you go about writing evaluation reward function recorder for multiagents as each agent gets its own local reward, how do you average not only for agents but for env as whole
14. check standard practice reward
15. RecordEpisodeStatistics doesn't work for multiagent as each agent gets its own reward, research and write to-do list
16. Sanity Test: test collision intentionally, observe what happens