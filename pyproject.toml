[project]
name = "multiagent-rware"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "yahiaabdelhady2023", email = "handyleaf11@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "gymnasium[classic-control,other]>=1.3.0",
    "ipykernel>=7.3.0",
    "matplotlib>=3.11.2",
    "notebook>=7.6.2",
    "pyvirtualdisplay>=3.0",
    "rware>=2.0.0",
]

[project.scripts]
multiagent-rware = "multiagent_rware:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
